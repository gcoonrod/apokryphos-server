// Phase 3 stub — manifest-parse satisfier. Real implementation lands per
// specs/003-fapi-dpop-auth-core/tasks.md (see the T??? task that creates this file).
// Gated behind required-features = ["test-utils"]; not compiled by default `cargo build`.
